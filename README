This project is loosely inspired by Jekyll and it generates a site from 
static pages.

---
Installation 

1. Clone git repository:
    git clone https://github.com/iuliachiriac/contribtest.git

2. Run init.sh:
    ./init.sh

3. Generate the site:
    ./generate.py test/source output
